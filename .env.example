# Use the backend computer's LAN address for a physical development device.
# Production must use HTTPS. This value is public application configuration.
EXPO_PUBLIC_API_URL=https://z0p76k6m-4000.inc1.devtunnels.ms
